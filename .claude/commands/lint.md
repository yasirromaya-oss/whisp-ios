---
description: Run SwiftLint on the project
allowed-tools: Bash(swiftlint *)
---

# Lint

Run SwiftLint on the project using the existing `.swiftlint.yml` configuration:
```bash
swiftlint lint --config .swiftlint.yml
```

Report any warnings or errors, grouped by severity.
