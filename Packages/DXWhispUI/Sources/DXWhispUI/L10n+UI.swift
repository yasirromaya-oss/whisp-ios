import Foundation

// MARK: - L10nUI — Localization Keys (DXWhispUI Package)

enum L10nUI {
    static let ok = String(localized: "ui_ok", defaultValue: "OK", bundle: .module)
    static let voiceNote = String(localized: "ui_voice_note", defaultValue: "Voice Note", bundle: .module)
}
